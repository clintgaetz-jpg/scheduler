// REPLACE lines 84-92 in QuoteBuilder.jsx
// This is the empty services state

        {services.length === 0 ? (
          <div className="text-sm text-gray-400 py-6 text-center">
            No services added yet
          </div>
        ) : (
